<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Dashboard | Create Task</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(to right top, #11998e, #38ef7d);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .task-container {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            box-shadow: 0 8px 32px rgba(31, 38, 135, 0.3);
            border-radius: 20px;
            padding: 35px;
            width: 480px;
            max-width: 95vw;
            color: #fff;
            animation: slideUp 0.8s ease;
        }

        .task-container h2 {
            text-align: center;
            margin-bottom: 25px;
            font-size: 26px;
            font-weight: 600;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 500;
            color: #f0f0f0;
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 12px 15px;
            border-radius: 12px;
            border: none;
            background-color: rgba(255, 255, 255, 0.2);
            color: #fff;
            font-size: 16px;
            transition: all 0.3s ease;
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            background-color: rgba(255, 255, 255, 0.3);
            box-shadow: 0 0 0 2px #ffffff70;
        }

        .form-group textarea {
            min-height: 120px;
            resize: vertical;
        }

        .create-btn {
            width: 100%;
            padding: 14px;
            border: none;
            border-radius: 12px;
            background: linear-gradient(90deg, #00c9ff, #92fe9d);
            font-size: 17px;
            color: #fff;
            font-weight: bold;
            cursor: pointer;
            transition: transform 0.2s ease;
        }

        .create-btn:hover {
            transform: scale(1.03);
            box-shadow: 0 0 12px rgba(255, 255, 255, 0.3);
        }

        .view-tasks-link {
            display: block;
            text-align: center;
            margin-top: 20px;
            font-size: 14px;
            color: #e0e0e0;
            text-decoration: none;
        }

        .view-tasks-link:hover {
            color: #ffffff;
            text-decoration: underline;
        }

        @keyframes slideUp {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body>
    <div class="task-container">
        <h2><i class="fa-solid fa-plus"></i> Create New Task</h2>
        <form action="/create-task" method="POST">
            <div class="form-group">
                <label for="title"><i class="fa-solid fa-heading"></i> Task Title</label>
                <input type="text" id="title" name="title" required placeholder="Enter task title">
            </div>
            <div class="form-group">
                <label for="content"><i class="fa-solid fa-align-left"></i> Task Content</label>
                <textarea id="content" name="content" required placeholder="Write your task details..."></textarea>
            </div>
            <button type="submit" class="create-btn">Create Task</button>
        </form>
        <a href="/tasks" class="view-tasks-link">View all tasks</a>
    </div>
</body>
</html>
